'use strict';

import * as vscode from 'vscode';

export class SupportedTextDocumentsProvider {
    public static retrieveTextDocuments(): vscode.TextDocument[] {
        //let files = vscode.workspace.findFiles('**/*.css', 'node_modules/**/node_modules/**/*');
        //files.
    }
}